<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--引入外部文件-->
    <!--<properties resource="database.properties"/>-->
    
    <settings>
        <setting name="logImpl" value="LOG4J"/>
    </settings>
    <!--给实体类取别名-->
    <typeAliases>
        <package name="com.wpt.pojo"/>
    </typeAliases>

    <!--配置数据库-->
    <!--<environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>-->

    <!--映射器  将包内的映射器接口实现全部注册为映射器-->
    <mappers>
<!--        <mapper resource="com/wpt/dao/bill/BillMapper.xml"/>-->
<!--        <mapper resource="com/wpt/dao/provider/ProviderMapper.xml"/>-->
<!--        <mapper resource="com/wpt/dao/role/RoleMapper.xml"/>-->
<!--        <mapper resource="com/wpt/dao/user/UserMapper.xml"/>-->
        <!-- 将包内的映射器接口实现全部注册为映射器 -->
        <!--<package name="com.wpt.dao"/>-->
    </mappers>

</configuration>